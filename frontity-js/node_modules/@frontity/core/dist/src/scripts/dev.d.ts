import "./utils/envs";
import { Mode } from "../../types";
declare const _default: ({ isHttps, mode, port, target, openBrowser, publicPath, }: {
    port: number;
    isHttps: boolean;
    mode: Mode;
    target: "module" | "es5";
    openBrowser?: boolean;
    publicPath: string;
}) => Promise<void>;
export default _default;
