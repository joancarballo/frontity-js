import "./utils/envs";
import { Mode } from "../../types";
declare const _default: ({ mode, target, publicPath, }: {
    mode: Mode;
    target: "both" | "module" | "es5";
    publicPath: string;
}) => Promise<void>;
export default _default;
