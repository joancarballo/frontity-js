/// <reference types="webpack" />
import { Target, Mode } from "../../../types";
declare const _default: ({ target, mode, outDir, publicPath, }: {
    target: Target;
    mode: Mode;
    outDir: string;
    publicPath: string;
}) => import("webpack").Output;
export default _default;
